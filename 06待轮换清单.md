# 06-VLM-Gemini-Vision 待轮换密钥 / 内网信息清单

> 整理时间：2026-06-16（Wave2 W3 感知抓取）
> 说明：本次只把源码/配置里的明文密钥与内网 IP 改为「从环境变量读取 + 占位符」，
> **未改动任何真实密钥的值，也未触碰正在运行的服务**。真实密钥是否泄露需用户自行判断并轮换。

## 一、需要用户轮换 / 确认的敏感项

| 项 | 原位置（改前） | 处理 | 用户动作 |
|---|---|---|---|
| OpenAI 兼容代理 API Key `cliproxy-ag-REDACTED` | `22_vlm_robot_test.py:41`、`23_vlm_full_test.py:48`（明文硬编码） | 已改为读 `VLM_API_KEY` 环境变量，源码中删除明文 | **建议立即轮换**：此 key 曾以明文存在于源码，且仓库为 git 子模块，可能已进入历史提交。轮换后只通过环境变量/.env 注入 |
| `vlm_config.json` 的 `api_key`（值为占位字符串） | `vlm_config.json:3` | 已清空为 `""`，改由环境变量提供 | 确认该值是否曾是真实 key；若是请轮换 |
| 内网代理 IP `192.168.1.102:8000` | `vlm_config.json:2` | 已清空 `api_base`，改由 `VLM_API_BASE` 提供 | 确认是否需要在文档/截图里继续暴露此内网地址 |
| 历史内网地址 `192.168.1.6:1034/3003/3002`、`localhost:8317`、`localhost:8000` | 多处文档/默认值 | 默认值保留 `localhost`（非敏感）；内网 IP 仅出现在 `测试结果和修复建议.md` 的排障示例中 | 如对外分享文档，建议把 `测试结果和修复建议.md` 中的 `192.168.1.102` 替换为占位 |

## 二、git 历史排查建议（由用户执行，本 worker 未做任何 git 写操作）

该子模块历史里很可能仍含明文 key。轮换后建议确认历史是否需要清理：

```bash
# 只读查看：搜索历史提交里是否出现过该 key（在 06 子模块目录内执行）
git log -p -S 'cliproxy-ag-REDACTED' -- . | head

# 如需彻底从历史抹除（高风险，会重写历史，务必先备份、且与协作者协调）：
#   git filter-repo --replace-text <(echo 'cliproxy-ag-REDACTED==>REDACTED')
```

## 三、本次已落地的安全改动（working-tree，未提交）

- `vlm_config.json`：`api_base`/`api_key` 清空为占位，新增 `_comment` 指向 env 用法。
- `22_vlm_robot_test.py` / `23_vlm_full_test.py`：`API_BASE`/`API_KEY` 改为 `os.environ.get(...)`，删除明文 key；OpenAI 客户端补 `timeout` + `max_retries`。
- `26_vlm_mujoco_grasp.py`：`load_config` 忽略空字符串/下划线注释字段（避免占位空值覆盖默认）；VLMClient 补 `timeout` + `max_retries`。
- 新增 `.env.example`（占位符）与 `.gitignore`（忽略 `.env`、`**/api_key`、`outputs/` 等）。

## 四、迁移到环境变量的标准用法

```bash
cp .env.example .env      # 然后编辑 .env 填真实值
export VLM_API_BASE="http://你的代理:端口/v1"
export VLM_API_KEY="你轮换后的新key"
export VLM_MODEL="gemini-3-pro-preview"
# 或在脚本启动前 source 一个不入库的 env 文件
```

> 提醒：`.env` 已被 `.gitignore` 排除；请确保真实 key 只存在于 `.env` 或 shell 环境，不再写回任何 `.json/.py`。
